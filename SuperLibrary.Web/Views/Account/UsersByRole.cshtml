@model IEnumerable<SuperLibrary.Web.Models.UserViewModel>
@{
    ViewData["Title"] = $"Manage Users";
}

<h1>Manage Users</h1>

<a asp-action="ManageUsers" asp-controller="Account" class="btn btn-secondary mb-3"><i class="fa-solid fa-arrow-left me-1"></i> Return</a>

@if (User.IsInRole("Admin"))
{
    <a asp-action="AddUserToRole" asp-controller="Account" asp-route-role="@ViewBag.Role" class="btn btn-success mb-3 ms-2"><i class="fa-solid fa-user-plus me-1"></i> Add User</a>
}

@if (!Model.Any())
{
    <div class="alert alert-info">No users found in this role.</div>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Username</th>
                <th>Email</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Phone Number</th>
                @if (User.IsInRole("Admin"))
                {
                    <th>Actions</th>
                }
            </tr>
        </thead>
        <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Username</td>
                <td>@user.Email</td>
                <td>@user.FirstName</td>
                <td>@user.LastName</td>
                <td>@user.PhoneNumber</td>
                @if (User.IsInRole("Admin"))
                {
                    <td>
                        <a asp-action="UserDetails" asp-controller="Account" asp-route-username="@user.Username" asp-route-role="@ViewBag.Role" class="btn btn-info btn-sm"><i class="fa-solid fa-eye me-1"></i> View</a>
                        <a asp-action="EditUserInRole" asp-controller="Account" asp-route-username="@user.Username" asp-route-role="@ViewBag.Role" class="btn btn-primary btn-sm"><i class="fa-solid fa-pen me-1"></i> Edit</a>
                        <form asp-action="RemoveUserFromRole" asp-controller="Account" method="post" style="display:inline">
                            <input type="hidden" name="username" value="@user.Username" />
                            <input type="hidden" name="role" value="@ViewBag.Role" />
                            <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to remove this user?');"><i class="fa-solid fa-user-minus me-1"></i> Remove</button>
                        </form>
                        @if (user.IsAdminConfirmed)
                        {
                            <span class="text-success">
                                <i class="fa-solid fa-circle-check"></i> Confirmed
                            </span>
                        }
                        else
                        {
                            <form asp-action="ConfirmUserInRole" asp-controller="Account" method="post" style="display:inline">
                                <input type="hidden" name="username" value="@user.Username" />
                                <input type="hidden" name="role" value="@ViewBag.Role" />
                                <button type="submit" class="btn btn-success btn-sm"><i class="fa-solid fa-check me-1"></i> Confirm</button>
                            </form>
                        }
                    </td>
                }
            </tr>
        }
        </tbody>
    </table>
}
